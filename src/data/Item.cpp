#include "src/data/Item.h"

Item::Item(std::string name) {
  // TODO
  // XML READER
}

/* TODO should be in character
bool Item::consume(Character * user) {
  if (consumable) {
    for (auto e : effects) {
      e->activate(user);
    }
    delete this;
    return true;
  }
  else {
    return false;
  }
}
*/
